is acceptedUsername(username){
var regex=/^[a-zA-Z]{3}[0-9]{2,6}$;
return regex.test(username);
}
if(isAcceptedUsername("Aa86kjl"){
alert("username is valid");
}else{
alert("username is invalid");
}

is acceptedpassword(password){
var regex=/^[a-zA-Z]{1}[a-zA-Z0-9]{7}$@;
}
if(isacceptedpassword("aA1mkm@m"){
alert("password is valid");}
else{
alert("password is invalid");}